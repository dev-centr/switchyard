name: tools
title: Tools
version: ~
# Nav owned by hub tools-docs (antora.yml nav list). Antora Object.assign keeps only the
# last source's nav for a shared component name; product nav: keys would clobber the forest.
